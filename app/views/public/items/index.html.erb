<div>
  <h4>商品一覧</h4><p>(全<%= @item.count %>件)</p>
</div>
<% @items.each do |item| %>
<div>
  <%= image_tag item.get_image(200,200) %>
  <%= item.name %>
  <%= item.price.to_s(:delimited) %>
</div>
<% end %>
<%= paginate @items %>