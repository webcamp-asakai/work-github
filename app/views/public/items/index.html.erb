<div>
  <h4>商品一覧</h4><p>(全<%= @item.count %>件)</p>
</div>
<% @items.each do |item| %>
<div>
  <%= link_to item_path(item.id) do %>
    <%= image_tag item.get_image(200,200) %>
  <% end %>
  <%= item.name %>
  <%= item.price.to_s(:delimited) %>
</div>
<% end %>
<%= paginate @items %>