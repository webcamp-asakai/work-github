<div class="container">
  <div class="row">
    <div class="col-lg-9 mx-auto">
      <h4 class="bg-light col-lg-2 m-5">ログイン</h4>
      <h5 class="bg-light col-lg-5 m-5">会員の方はこちらからログイン</h5>

      <%= form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>

      <div class="d-flex mb-3">
        <p class="col-lg-3">メールアドレス</p>
        <div class="flex-fill">
          <%= f.email_field :email, autofocus: true, autocomplete: "email", placeholder: "sample@example.com", class:"col-lg-7" %>
        </div>
      </div>

      <div class="d-flex mb-3">
        <p class="col-lg-3">パスワード</p>
        <div class="flex-fill">
          <%= f.password_field :password, autocomplete: "current-password", class:"col-lg-7" %>
        </div>
      </div>

      <div class="actions">
        <div class="m-5">
          <%= f.submit "ログイン", class:"btn btn-primary px-5" %>
        </div>
      </div>
      <% end %>

    <div>
    <h4 class="bg-light m-5 col-lg-5">会員登録がお済みでない方</h4>
    <p class="ml-5">
      <%= link_to "こちら", new_customer_registration_path %>から新規登録を行なってください。
    </p>
    </div>

